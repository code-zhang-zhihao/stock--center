# 行情数据模块

## 相关源码

- `python-back/app/core/config.py`
- `python-back/app/db/session.py`
- `python-back/app/main.py`
- `python-back/app/modules/market_data/api.py`
- `python-back/app/modules/market_data/service.py`
- `python-back/app/modules/market_data/sync_service.py`
- `python-back/app/modules/market_data/scheduler_handlers.py`
- `python-back/app/modules/market_data/providers.py`
- `python-back/app/modules/market_data/repository.py`
- `python-back/app/modules/market_data/models.py`
- `python-back/app/modules/market_data/schemas.py`
- `python-back/scripts/check_market_data_providers.py`
- `start.sh`

## 当前能力

| API | 说明 |
| --- | --- |
| `GET /api/v1/market-data/query/stock` | 股票基础资料，多源补齐。 |
| `GET /api/v1/market-data/query/daily-bars` | 日线，多源补齐并写入 `t_daily_bar`。 |
| `GET /api/v1/market-data/query/minute-bars` | 分钟线，多源补齐并写入 `t_minute_bar`。 |
| `GET /api/v1/market-data/query/quote` | 实时 quote，多源补齐并写入 `t_quote_snapshot`。 |
| `GET /api/v1/market-data/query/ticks` | 分笔成交，多源补齐并写入 `t_tick_trade`。 |
| `GET /api/v1/market-data/query/sectors` | 板块/概念/行业基础信息，写入 `t_sector_basic`。 |
| `GET /api/v1/market-data/query/sector-components` | 板块成分股，写入 `t_sector_component`。 |
| `GET /api/v1/market-data/query/stock-sectors` | 按股票反查所属板块/概念/行业，读取 `t_sector_component` 和 `t_sector_basic`。 |
| `GET /api/v1/market-data/query/sector-bars` | 板块行情，写入 `t_sector_bar`。 |
| `GET /api/v1/market-data/query/indexes` | 指数基础信息，写入 `t_index_basic`。 |
| `GET /api/v1/market-data/query/index-components` | 指数成分股，写入 `t_index_component`。 |
| `GET /api/v1/market-data/query/index-bars` | 指数行情，写入 `t_index_bar`。 |
| `GET /api/v1/market-data/query/fund-flow` | 个股或板块资金流，写入资金流事实表。 |
| `GET /api/v1/market-data/query/lhb` | 龙虎榜事件和席位明细。 |
| `GET /api/v1/market-data/query/announcements` | 上市公司公告。 |
| `GET /api/v1/market-data/query/indicators` | 查询已落库因子、技术指标快照和指标定义。 |

## 注意

不要把策略、Skill、LLM 逻辑混入本模块；行情模块只负责事实数据查询、批量同步、raw landing、canonical upsert 和指标查询入口。

`MarketDataQueryService` 负责单次查询和 provider fallback；`MarketDataSyncService` 负责批量同步任务编排、进度摘要和异常汇总。调度中心只触发 handler，不承载行情业务逻辑。

## 批量同步任务

行情模块向 `JobHandlerRegistry` 注册两个真实任务：

| `job_code` | 说明 | 默认 cron | 默认状态 |
| --- | --- | --- | --- |
| `sync_sector_catalog` | 同步概念/行业板块目录，并维护 `t_sector_component` 股票关联关系。 | `20 8 * * 1-5` | disabled |
| `sync_stock_basic` | 每周同步 A 股基础资料，AkShare 主源，MooTDX fallback。 | `30 8 * * 0` | disabled |

任务 seed 来源：

```text
docs/sql/10-market-data-sync-jobs.sql
```

板块同步规则：

- 默认同步 `concept` 和 `industry`。
- AkShare EastMoney 接口优先读取板块列表和成分股。
- EastMoney 板块列表失败时 fallback 到 AkShare 同花顺接口：`stock_board_concept_name_ths` / `stock_board_industry_name_ths`。
- 同花顺 fallback 的 `sector_code` 使用 `ths_concept_{code}` / `ths_industry_{code}`，避免和 EastMoney `BKxxxx` 冲突，原始 code 保留在 `metadata_json.raw_code`。
- 同花顺 fallback 的成分股第一版解析同花顺详情页第一页成分表，source 为 `akshare:stock_board_*_detail_ths_html`；后续可继续补分页接口。
- `t_sector_basic` 和 `t_sector_component` 只 upsert，不物理删除历史板块或历史关联。
- 完整同步某个板块后，本次未出现的旧成分标记 `end_date`。
- `limit_sectors`、`max_concurrency`、`provider_timeout_seconds` 可用于小范围 smoke。
- 当前环境如果访问 AkShare/EastMoney 失败，任务必须明确 failed，不能以 0 条 success 静默通过；具体 provider 异常写入 `t_scheduler_job_run.result_summary.errors`。

股票基础资料同步规则：

- AkShare 主源读取全量 A 股列表，返回数量低于 `min_expected_count` 时视为异常。
- AkShare 失败或数量异常时可 fallback 到 MooTDX。
- MooTDX fallback 只补代码、名称、交易所、状态等基础字段，不覆盖 AkShare 已有细节。
- `include_detail=true` 时渐进补充行业、上市日期、更名信息，默认每次最多 300 只。
- 不删除 `t_stock` 已有记录；只有 AkShare 明确返回退市列表时才标记 `status=delisted`。
- 同步 raw 响应写入 `t_provider_raw_record`，用于排查 provider 返回内容。

## 运行配置

数据库连接沿用 `stock-analysis` 的连接池参数：`DATABASE_POOL_SIZE`、`DATABASE_MAX_OVERFLOW`、`DATABASE_POOL_TIMEOUT_SECONDS`、`DATABASE_POOL_RECYCLE_SECONDS`、`DATABASE_ECHO_POOL`。

跨域配置使用 `CORS_ALLOW_ORIGINS`，多个 origin 用英文逗号分隔。

国内行情源默认不走系统代理。后端启动时会把 `MARKET_DATA_NO_PROXY_DOMAINS` 合并到 `NO_PROXY/no_proxy`，默认包含：

```text
eastmoney.com,.eastmoney.com,10jqka.com.cn,.10jqka.com.cn,iwencai.com,.iwencai.com,sina.com.cn,.sina.com.cn,tdx.com.cn,.tdx.com.cn
```

MooTDX 不再暴露环境变量，默认在代码中使用 `market=std`、`timeout=30`、`auto_retry=3`，并从 MooTDX 自带 `HQ_HOSTS` 中按顺序尝试候选服务器。

## MooTDX 兼容策略

- quote、日线、分钟线优先通过 MooTDX `quotes` 客户端读取；某个服务器连接或调用失败时，切换到下一个 `HQ_HOSTS` 候选。
- 分笔成交通过 MooTDX `transaction/transactions` 读取，适合实时行情展示和微观结构指标。
- 指数行情 fallback 通过 MooTDX `index` 读取。
- 板块信息 fallback 通过 MooTDX `block` 读取，但 AkShare 仍是行业/概念主源。
- 股票基础资料 fallback 使用 MooTDX 股票列表能力补齐，只返回命中股票的基础字段，避免把全市场列表写入 raw。
- MooTDX 分钟线返回的是交易日内序列点，不一定携带标准时间字段；当前按 A 股连续竞价时间映射为 `09:31-11:30`、`13:01-15:00`，再规范化写入 `t_minute_bar.bar_time`。
- 外部响应写入 `t_provider_raw_record.raw_payload` 前会清理字符串中的 NUL 字符，避免 PostgreSQL JSONB 写入失败。

## AkShare 主源能力

- `stock_board_*`：板块、概念、行业基础信息、成分股和行情。
- AkShare 板块列表可能在同一批响应中出现重复 `sector_code`，写入 canonical 表前必须按唯一键去重。
- `stock_zh_index_*`、`index_stock_*`：指数基础、指数行情和成分股。
- `stock_individual_fund_flow`、`stock_fund_flow_industry`、`stock_fund_flow_concept`：个股和板块资金流。
- `stock_lhb_*`：龙虎榜事件和席位明细。
- `stock_zh_a_disclosure_report_cninfo`：上市公司公告。
- `stock_zh_a_tick_tx_js`：历史分笔成交 fallback。

Provider 验证使用：

```bash
cd python-back
source .venv/bin/activate
python scripts/check_market_data_providers.py --stock-code 600519 --provider all
```
