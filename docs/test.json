我理解的层级关系是：
搜索：
    问财：
        key1
        key2
    妙想：
        key1
        key2
    kimi:
        key1
        key2
llm:
    kimi:
        key1
        key2
    aliyun_codingplan:
        key1
        key2
    ...
通知：
    飞书
    自定义
    邮箱
    

然后：针对 llm/通知 有 options 表， 来定义各自的参数


使用的时候：
    问财skill入口 -> 查询问财的key 根据入参 调用指定的技能 -> 判断结果是否是 key失效  如果失效 使用 下一个 有效的key ，，， -> 返回结果
    妙想同理
    kimi的搜索 只是一个网页搜索的入口对吧，你看看官方文档是如何使用的

    llm调用入口 -> 如果有指定的llm模型使用指定的，如果没有指定使用默认的llm（配置界面可配置具体模型为默认模型） -> 调用具体的llm模型（填充配置项）理论上每个模型单独一个service -> 返回结果
        注意：1个模型（比如 DeepSeek/ 阿里云coding plan /火山云coding plan） 这种的他就只有一个指定的编码，这样才对，
    通知调用入口 参考 llm这种

也就是是说，总体逻辑为 工厂模式： 调用具体的skill / llm 或者 通知


这样的话配置是不是就简单了呢。
我理解应该就是几个表：
1. t_system_config  表  ID 分类编码 （搜索，llm,通知） 编码 ：（问财/妙想。。。） 编码名 创建时间 更新时间。是否启用  是否默认（每个分类编码只有一个默认）
2. t_config_value 表 id 配置id value 名称 创建时间 更新时间。是否启用 优先级 
3. t_config_option 表 ID 参数名 编码 当前值 是否启用 创建时间 更新时间

问财skill入口 查询的时候：问财他就根据问财的编码（直接定义死）去查询key列表 就好了，属于前后端约定好的
妙想同理

llm/通知就需要额外查询 option表这个样子

入口只有一个，然后入口进去 查询相关的表，根据需求传递给具体的服务，这才是一个合理的逻辑，同时每个具体的服务单独一个文件，指的是 llm工厂入口文件（查询参数等，根据需求传递给下一层） 具体的llm一个文件（可能每个模型的参数等不太相同，或者调用方式不太相同)（是否需要单独一个文件这个你作为专业的python开发分析一下是否需要）