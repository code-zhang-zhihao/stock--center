# 变更记录

| 日期 | 类型 | 摘要 | 影响范围 | 兼容性/迁移 |
| 2026-07-28 | 情绪模型/校准可视化 | 情绪模型页新增完整基线双分曲线、缩放时间轴、五阶段连续时间带与降级日提示；`emotion-daily` 新增可控的 `history_limit`（默认 60、20–1000），模型页按校准长度读取，盘后页保持 60 日默认。 | `market_insight` API/服务、情绪模型前端、回归测试、Wiki。 | 无 SQL 迁移；已完成的基线可直接查看，模型仍需管理员确认启用后才进入日常 22:15 计算。 |
| 2026-07-28 | V2 情绪校准缺陷修复 | 修复基线校准首次写入 `calibration_summary` 时，把 Python `date` 直接传给 JSONB 而导致任务在加载输入前失败的问题；首尾交易日现统一写为 ISO 日期字符串。 | `market_insight.emotion_service`、V2 情绪回归测试、Wiki。 | 无 SQL 迁移；部署后手动重新运行失败的 `emotion_mode=baseline` 任务即可，旧失败运行和 0 行结果不需清理。 |
| 2026-07-28 | 盘后情绪模型/Schema/API/前端 | 新增 V2 可配置双分情绪模型：短线接力与大盘风险偏好分别按 120 日滚动分位评分，保存原始值、权重、贡献、来源、数据质量与五阶段（冰点/修复/活跃/高潮/退潮）+辅助状态证据。新增模型管理/每日查询 API、`/emotion-models` 页面和盘后复盘双分区；V1 表与 `/daily-sentiment` 保持兼容。21:30 新增市场级北向资金流事实，北向持仓/两融按实际披露日仅作辅助确认。 | `market_insight`、`market_data.close_ingest`、调度、盘后复盘、情绪模型页、Wiki。 | 需在 `66` 后执行 `docs/sql/67-market-emotion-v2.sql` 并 reload Scheduler。迁移先 seed 默认草稿，不会自动启用：管理员需创建/确认草稿、手动启动 `emotion_mode=baseline` 校准，基线完整后再启用。现有 canonical/Derived 事实和 V1 历史结果不删除。 |
| --- | --- | --- | --- | --- |
| 2026-07-28 | 实时五档缓存缺陷修复 | 修复 TickFlow 五档响应中的 `depth_time` 为 `datetime` 时，Redis `set_many_json` 无法序列化并导致整批快照写入内存回退的问题。Redis JSON 边界现统一支持日期时间、Decimal、Enum、UUID 与标量 `.item()` 值；Quote、五档及分钟 Hash 缓存共用该规则。 | `core.redis_client`、`realtime_market`、Redis 缓存测试、Wiki。 | 无 SQL 或数据迁移；部署后新一轮五档刷新会自然覆盖/补齐 30 秒 TTL 的缓存键。 |
| 2026-07-28 | V2 情绪校准性能/历史事实 | 新增手动 `backfill_market_north_flow`：只对已有日线的最近 250 个开市日按 120 日窗口调用 `moneyflow_hsgt`，仅补 `north_money` 缺失日并保留每窗 Raw 审计。V2 基线由 620 自然日预取收敛为精确 120 个开市日预热，上市交易日门槛先在股票 CTE 求解；20 日创新高/新低改为每 20 个交易日一组的有界索引查询。八个输入块、市场子批次和每 20 日写入检查点会出现在调度运行摘要，超时保留最后进度。 | `market_data`、`market_insight`、`scheduler_center`、调度定义、Wiki。 | 先执行 `docs/sql/68-market-emotion-baseline-performance.sql`、部署后端并 reload Scheduler；再运行北向历史回填后启动 V2 `emotion_mode=baseline`。北向持仓/两融继续为延迟确认，不需作为校准前置；不删除既有事实或 V1/V2 行。 |
| 2026-07-28 | 盘后报告事实/数据质量 | 将既有 22:15 `calculate_market_daily_sentiment` 扩展为“生成每日市场报告事实”：除 `t_market_sentiment_daily` 外新增 `t_market_sector_heat_daily/t_market_limit_up_evidence_daily`、`GET /api/v1/market-insights/daily-review` 和盘后页热点/龙头/涨停关联证据区。概念热度直接从成分股日线、资金流、涨停事件聚合，避免 `ths_daily` 晚发布；龙虎榜与已沉淀公告严格标为关联证据，不作涨停因果。修复 `ths_daily` 返回 0 行仍写 Raw `captured` 的问题：未来 Raw 记录标为 `failed`（保留专用错误码），增强块标为 `deferred`，迁移会修复既有零行审计记录。 | `market_insight`、`market_data.close_ingest`、调度、盘后市场复盘页、Wiki。 | 需在 `65` 后执行 `docs/sql/66-market-daily-review-facts.sql` 并 reload Scheduler。任务默认新增报告事实；大范围历史情绪基线回填可传 `include_report_facts=false`。不修改 canonical 事实；公告/新闻全量同步、新闻归因、LLM 分析、策略和交易建议仍未实现。 |
| 2026-07-28 | 盘后市场情绪/Derived | 新增 `market_insight` 模块、只读 `GET /api/v1/market-insights/daily-sentiment`、`t_market_sentiment_daily` 与 22:15 `calculate_market_daily_sentiment`。`v1` 仅以 canonical 日线、涨跌停/炸板、交易日历和 Raw 完成证据计算 0–100 分及冰点/退潮/混沌/修复/活跃/主升阶段；保存全部原始分项、权重、公式、覆盖和版本。日线覆盖低于 95% 或涨跌停 Raw 未完成时写 `pending`，不生成伪分数。盘后页展示该结果，但页面不会触发计算。 | `market_insight`、调度注册、盘后市场复盘页、Wiki。 | 需执行 `docs/sql/65-market-daily-sentiment.sql` 后 reload Scheduler；只新增 Derived 表、索引和任务定义，不改写 canonical 事实或历史任务运行记录。首次需要阶段基线时手动按日期范围回填 `v1`。LLM、新闻归因、策略与交易建议未实现。 |
| 2026-07-28 | 前端/信息架构 | 将盘后事实从盘中 `/market-overview` 拆为独立 `/post-close-market` 页面和导航入口。盘中页只保留统一 Quote 轮次、SSE 与盘中排行；盘后页独立每 60 秒读取既有 `/realtime/post-close-structure`，展示完成日的涨跌停、炸板、封板率及连板梯队。 | `web-admin` 路由、导航、实时市场总览、盘后市场复盘页和 Wiki。 | 无 API、SQL 或数据迁移；复用既有日频事实和 300 秒/60 秒服务端缓存。规则情绪分随后由同日 `market_insight` 变更补齐；策略建议仍未实现。 |
| 2026-07-27 | 盘后市场事实/性能 | 新增只读 `GET /api/v1/realtime/post-close-structure` 与市场总览“涨停梯队/炸板观察”区。以最新 `t_daily_bar` 日期为准，先校验 `limit_list_d` Raw 完成记录，按 `t_trade_calendar` 连续开市日的 `t_limit_event_daily.limit_up` 计算连板；`limit_break` 单独统计，封板率为涨停÷（涨停+炸板）。无完成标记时明确返回未完成，不将零行误判为无事件。修正最新日定位查询，避免先关联全量日线历史导致首读约 31 秒；真实库只读探针降至约 4.3 秒。完成结果缓存 300 秒，未完成结果缓存 60 秒，页面每分钟检查晚间增强数据。 | `realtime_market`、实时市场总览页面、日频事件事实读取。 | 无需 SQL 或迁移；依赖每日/历史任务保留 `t_provider_raw_record` 完成记录。该能力不是情绪分、新闻解释或交易信号。 |
| 2026-07-27 | 实时行情/前端 | 新增独立“实时市场总览”页面和同轮缓存接口：全市场宽度、可验证涨跌停、七大指数、开盘后/日内结构、涨跌/成交额/成交量榜、概念题材主层、申万行业辅助层、股票池、盘中宽度时间线与短期变化事件。概念热度输出涨跌/扩散/可验证涨跌停/成交额构成和覆盖率置信度；MA5/20/60 使用最新已收盘日 `system:daily_close` 因子作参照，不用未收盘行情伪算。时间线/事件只短期保存在 Redis/内存，不写 PostgreSQL，也不新增任何 TickFlow 请求。新增 `/realtime/market-timeline`、`/market-events`、`/pools` 和对应 SSE topics。 | `realtime_market`、`web-admin` 实时市场总览、Wiki。 | 无需 SQL 或数据迁移；需已有实时运行时和概念成分关系。TickFlow 申万目录/成员尚未同步时，行业页会明确显示不可用；盘后情绪、策略、LLM 和交易建议仍未实现。 |
| 2026-07-27 | 调度/数据资产 | 将 `limit_list_d/suspend_d`（涨跌停/炸板、停复牌）和 `ths_daily`（板块日线）从 18:00 `daily_close_core_ingest` 移至 21:30 `daily_close_enrichment_ingest`，避免数据尚未发布时把核心任务拖成 partial。事件和板块日线改为增强完整性项：18:00 核心数据齐全时报告可标记 `degraded`，晚间事实补齐后转为完整；次日修复仍按缺口补数；回归测试覆盖两个块的实际阶段归属和完整性分级。 | `market_data.close_ingest`、每日收盘 handler、调度 SQL、完整性状态与回归测试。 | 已执行旧四级流水线的数据库需执行 `docs/sql/64-daily-close-late-facts-enrichment.sql` 并 reload Scheduler；不删除任何事实或历史任务运行记录。 |
| 2026-07-27 | 数据资产/性能/缺陷修复 | `daily_close_core_ingest` 的七个当日核心指数日 K 改为 TickFlow `klines` 单指数优先（最多 3 并发、每个 2 根未复权日 K），只有 TickFlow 缺失当日 bar 的代码才回退一次 Tushare `index_daily`；拒绝旧交易日 bar，移除该核心路径的 AkShare/MooTDX 回退及其无效节点扫描。历史指数回填仍使用 Tushare。 | `market_data.close_ingest`、`realtime_market.tickflow_runtime`、核心收盘任务测试。 | 无需 SQL 或迁移；TickFlow Key 仍需先通过既有实时能力测试。任务会记录最终每行 Provider，TickFlow/Tushare 审计摘要分开保存。 |
| 2026-07-27 | 股票池交互/API | 股票池的实时监控设置从详情内联控件收口到“新建 / 编辑 / 配置”弹窗；详情改为只读业务摘要，系统预置池可配置启停和实时方式但不能改名或删除。`POST/PATCH` 新增可选嵌套 `realtime_policy`，池状态和策略在同一事务保存；保留既有单独策略接口以兼容程序化调用。 | `stock_pool` Schema/服务、股票池前端、服务测试。 | 无需新迁移；已有策略不改写。动态 `all_a_share` 仍不能启用实时目标策略。 |
| 2026-07-27 | 实时行情/缓存/前端 | 实时底座收口为池策略驱动：新增一对一股票池实时策略，移除分钟目标按代码补齐全市场的行为；TickFlow 全市场 Quote 改为 `CN_Equity_A` 单请求轮询，热/温/指数/按需请求按 90% 额度预算分配，五档与分钟线拆为独立不可重入块。MooTDX 分钟线维持 500 登记/200 分钟保障，Redis 改为分钟 Hash 增量写入；市场、题材、行业和股票池聚合严格使用同一全市场 Quote 轮次，数据中心新增实时市场总览与分钟目标分布。 | `realtime_market`、`stock_pool`、`RedisClient`、数据中心/股票池页面。 | 需由 owner/DBA 执行 `docs/sql/63-realtime-runtime-stabilization.sql`，随后重启后端或等待参考数据刷新。历史事实、分钟线与分钟因子不删除；不接 TickFlow WebSocket、不做自动交易。 |
| 2026-07-27 | 数据资产/性能/缺陷修复 | 修复 Redis 可用时 `set_json` 未实际写入的问题；数据中心改为 current + last-good 双快照、Redis 单飞租约和缓存构建中响应，页面不再因缓存失效同步扫描大表。资产总览把最新日覆盖率合并为一次批量计算，大型日频表行数改为 PostgreSQL 统计估算并明确标识。 | `core.redis_client`、`data_assets`、数据中心页面、缓存状态。 | 部署后首次访问会后台预热；执行 `docs/sql/62-data-assets-cache-performance-indexes.sql` 可优化最新日覆盖率，并需由表 owner 在非事务会话执行。旧缓存键可自然过期，不删除业务事实。 |
| 2026-07-25 | 实时行情/配置/前端 | 实时能力改为按数据类型路由：TickFlow 提供全市场与单股实时 Quote，MooTDX 保留为实时分钟线唯一 Provider；Quote 覆盖集合竞价时段，分钟线只在连续竞价刷新。配置中心新增 TickFlow 加密 API Key、连通性测试与来源展示，实时状态返回 Quote/分钟线 Provider。 | `realtime_market`、`config_center`、系统设置中心、实时 SSE/缓存。 | 执行 `docs/sql/60-tickflow-realtime-quote.sql`，先新增并测试 active TickFlow `api_key`，再启用实时行情运行时。无 Key 时明确报错，不会回退 Quote 到 MooTDX；`t_minute_bar`、分钟因子和已有缓存结构不删除。 |
| 2026-07-25 | 调度/Schema/前端 | 新增调度标签目录与任务-标签多对多关系；调度中心可按历史、每日、主数据、策略筛选并展示标签。移除 smoke test、旧全市场收盘兼容入口和无 handler 的 Tushare 专题任务；页面改为默认仅展示已启用任务，并可切换到已暂停或全部状态。 | `scheduler_center`、调度中心页面、任务定义。 | 执行 `docs/sql/59-scheduler-job-tags-and-retire-obsolete-jobs.sql`，部署前后端后 reload Scheduler。只删除三条任务定义及其标签关联，`t_scheduler_job_run` 历史运行日志保留。 |
| 2026-07-24 | 性能/缺陷修复 | 核心指数日线从 7 次逐指数 Tushare 请求改为 1 次按交易日批量请求；AkShare 缺失回退改为并发且单调用最多等待 5 秒，MooTDX 回退限制为一次 4 节点 × 2 秒扫描，空响应不再缓存，并显式关闭连接以消除 `BaseQuotes.__del__` 重复断连。 | `market_data.close_ingest`、`MootdxProvider`、`daily_close_core_ingest` 指数数据块。 | 无需 SQL；当日 `index_daily` 尚未发布且其他源不可用时仍保留真实缺失，不用上一交易日冒充。实测全源不可用路径由约 7 分钟降至约 8 秒。 |
| 2026-07-24 | 性能/调度/数据资产 | 每日收盘升级为 15:30 分钟、18:00 核心、21:30 增强、次日 08:00 缺口修复四级流水线；分钟因子继续持久化但改为 200 股票分片集合计算，板块日线改为 500 代码批量请求，日频因子/快照改为集合计算，增强只定向合并专业技术字段。 | `market_data.close_ingest`、`indicator_engine`、调度任务、数据资产生产关系与运行摘要。 | 已幂等执行 `docs/sql/58-daily-close-four-stage-pipeline.sql` 两次并 reload Scheduler；不删除现有事实。分钟线与分钟因子保留 30 个交易日，旧 `daily_market_close_ingest` 继续隐藏。 |
| 2026-07-20 | 个股行情/缺陷修复 | 日 K 页面序列按返回的同一批交易日附带 MA5/10/20/30/60 轻量投影，修复 250 根 K 线只拼接 120 天因子而导致均线只显示后半段的问题；页面因子聚合不再返回多日完整专业因子 JSON。 | `stock-analysis/daily-bars`、`stock-analysis/factors`、`StockMarketWorkbench.vue`、日 K 页面查询契约。 | `daily-bars` 响应项增加可空 `ma5/ma10/ma20/ma30/ma60`；`factors` 的专业技术因子/快照/筹码数组收敛为最新一条。无需 SQL 或历史重算，部署前后端即可。 |
| 2026-07-20 | 数据资产/调度 | `backfill_stock_daily_facts` 新增按交易日查询的 `limit_list_d/suspend_d` 阶段，补齐 `t_limit_event_daily`，用 Raw 完成标记支持事件日期级安全续跑；`Z` 池映射为 `limit_break`，类别为空但带 `up_stat` 的历史 U 池响应映射为 `limit_up`。 | `market_data.history_backfill`、Tushare 日频 Adapter、数据资产生产关系、历史板块因子前置数据。 | 执行 `docs/sql/57-stock-history-limit-events.sql`、部署后端并 reload Scheduler；脚本幂等归一旧 Z/U 池通用事件，不改表结构和旧运行记录。 |
| 2026-07-19 | 主数据/缺陷修复 | `sync_stock_basic` 的退市名称识别从仅支持 `退市*` 扩展为 `退市*`、`*退`、`*(退)`、`*（退）`；基础资料 Upsert 可自动把 Provider 仍报 `L` 的退市过渡股票更新为 `delisted`。 | `market_data.sync_service`、股票主数据动态范围。 | 无需 SQL；部署后手动运行一次或等待下一次 `sync_stock_basic`。不删除 `t_stock_pool_member` 和历史事实。 |
| 2026-07-18 | 性能/调度 | 板块历史日 K 从交易日级退化串行改为逐板块一次完整区间请求，默认 12 个板块 worker；板块资金流按实测对账后的 20 交易日窗口、独立 2 worker 执行。 | `market_data`、`backfill_sector_daily_facts`、运行参数与 Wiki。 | 执行 `docs/sql/56-sector-history-range-backfill.sql` 并 reload Scheduler；不改事实表结构。 |
| 2026-07-18 | 数据资产/调度/Schema | 历史初始化收敛为个股、板块、指数各“日频事实 + 日频因子”两段；新增指数因子表、个股 MA30/MA60，历史技术快照不再生成分钟因子。 | `market_data`、`indicator_engine`、`data_assets`、调度任务、个股行情前端。 | 执行 `docs/sql/55-data-asset-history-pipelines.sql` 并 reload Scheduler；7 个旧任务定义被删除，旧运行日志保留。 |
